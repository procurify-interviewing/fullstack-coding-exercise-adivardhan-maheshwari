import * as React from "react";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import PurchaseRequestApprovals from "../components/App";

describe("Purchase request approvals", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });
  it("should...", () => {
    console.log("add your tests here");
  });
});
